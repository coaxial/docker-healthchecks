upstream app {
  ip_hash;
  server hc:8000;
}

server {
  listen 8080;
  server_name localhost;

  location /static/ {
    autoindex on;
    alias /static/;
  }

  location / {
    proxy_pass http://app/;
  }
}
